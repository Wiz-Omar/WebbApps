// Base URL for the API
export const API_BASE_URL = "http://localhost:8080";

// Base URL for the image API
export const IMAGE_ENDPOINT = `${API_BASE_URL}/image`;

// Base URL for the like API
export const LIKE_ENDPOINT = `${API_BASE_URL}/like`;

// Base URL for the user API (login)
export const LOGIN_ENDPOINT = `${API_BASE_URL}/user/login`;

// Base URL for the user API (register)
export const REGISTER_ENDPOINT = `${API_BASE_URL}/user`;
